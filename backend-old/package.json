{
  "name": "dashrunner-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DashRunner API — Express + Knex (Tycoon-style layout, lighter stack)",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "migrate": "mkdir -p data && knex migrate:latest --knexfile knexfile.js",
    "migrate:rollback": "knex migrate:rollback",
    "migrate:make": "knex migrate:make"
  },
  "dependencies": {
    "@privy-io/server-auth": "^1.32.5",
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.4",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^7.2.0",
    "knex": "^3.1.0"
  }
}
